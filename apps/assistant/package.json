{
  "name": "assistant",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --env-file .env uvicorn src.app.main:app --reload --port 8000",
    "start": "uv run --env-file .env uvicorn src.app.main:app --port 8000",
    "test": "uv run pytest",
    "typecheck": "echo 'python — no tsc'"
  }
}
